# Build MoritApp for Windows, macOS, and Linux.
# - Tag push (v1.0.0): build all platforms + attach to GitHub Release
# - Manual: Actions → Build → Run workflow
name: Build

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: win
            args: --win --x64
          - os: macos-latest
            platform: mac
            args: --mac
          - os: ubuntu-latest
            platform: linux
            args: --linux --x64

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build (${{ matrix.platform }})
        env:
          # Unsigned Mac builds are fine for GitHub download distribution
          CSC_IDENTITY_AUTO_DISCOVERY: "false"
        run: npx electron-builder ${{ matrix.args }} --publish never

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: moritapp-${{ matrix.platform }}
          path: |
            dist/*.exe
            dist/*.exe.blockmap
            dist/*.dmg
            dist/*.zip
            dist/*.AppImage
            dist/*.deb
            dist/*.yml
            dist/*.yaml
          if-no-files-found: warn

  release:
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (install.ps1)
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          merge-multiple: true

      - name: Add Windows install.ps1 to release assets
        run: |
          cp install.ps1 release-assets/install.ps1
          find release-assets -type f | sort

      - name: Attach to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: release-assets/*
          generate_release_notes: true
          fail_on_unmatched_files: false
          body: |
            ## Install (Windows)

            PowerShell one-liner:

            ```powershell
            irm https://raw.githubusercontent.com/kgs142c/MoritApp/main/install.ps1 | iex
            ```

            Or from this release:

            ```powershell
            irm https://github.com/kgs142c/MoritApp/releases/latest/download/install.ps1 | iex
            ```

            Mac/Linux terminal install is set aside for later — use the `.dmg` / AppImage / `.deb` assets below.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
